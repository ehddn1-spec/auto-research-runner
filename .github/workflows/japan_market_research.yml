name: Daily Research Automation

on:
  schedule:
    - cron: '0 21 * * *'   # 한국시간 오전 6시
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Fetch News
        run: echo "News collected"

      - name: Generate Report
        run: echo "Report generated"
